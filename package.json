{
  "name": "check-runners-availability",
  "version": "1.0.2",
  "description": "GitHub Action that checks if self-hosted runners are online and cancels the workflow if none are available.",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "test": "echo \"no tests yet\""
  },
  "keywords": [
    "github-actions",
    "self-hosted",
    "runners"
  ],
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3",
    "typescript": "^5.6.3"
  }
}
